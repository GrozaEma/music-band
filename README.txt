READ ME


You need http-server to serve static files, in order to use the web app.
Run: npm install http-server -g
To start the server: node /usr/local/bin/http-server

(/usr/local/bin/   -  path where the http-server was installed)